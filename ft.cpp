#include<iostream>
using namespace std;

struct node {
    int data;
    struct node *next;
};

int main()
{
	struct node *temp;
	cin>>temp->data;
	 temp = (struct node *)malloc(sizeof(struct node));
     temp->next = 1024;
     cin>>temp->next;
     cout<<endl;
     cout<<temp->next;
    }
